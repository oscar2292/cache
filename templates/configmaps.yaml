apiVersion: v1
kind: ConfigMap
metadata:
  name: node-local-dns
  namespace: kube-system
  labels:
    addonmanager.kubernetes.io/mode: Reconcile
data:
  Corefile: |
    {{ .Values.dns-cache.dnsDomain }}:53 {
        errors
        cache {
                success 9984 30
                denial 9984 5
        }
        reload
        loop
        bind {{ .Values.dns-cache.localIp }} {{ .Values.dns-cache.dnsServer }}
        forward . __PILLAR__CLUSTER__DNS__ {
                force_tcp
        }
        health {{ .Values.dns-cache.localIp }}:8080
        }
    in-addr.arpa:53 {
        errors
        cache 30
        reload
        loop
        bind {{ .Values.dns-cache.localIp }} {{ .Values.dns-cache.dnsServer }}
        forward . __PILLAR__CLUSTER__DNS__ {
                force_tcp
        }
        }
    ip6.arpa:53 {
        errors
        cache 30
        reload
        loop
        bind {{ .Values.dns-cache.localIp }} {{ .Values.dns-cache.dnsServer }}
        forward . __PILLAR__CLUSTER__DNS__ {
                force_tcp
        }
        }
    .:53 {
        errors
        cache 30
        reload
        loop
        bind {{ .Values.dns-cache.localIp }} {{ .Values.dns-cache.dnsServer }}
        forward . __PILLAR__UPSTREAM__SERVERS__
        }